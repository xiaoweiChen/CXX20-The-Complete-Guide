

\mySamllsection{转发引用，forwarding reference}

C++标准用于通用引用的术语。

\mySamllsection{全特化，full specialization}

不再依赖于模板参数(主)模板的替代定义。

\mySamllsection{函数对象，function object (函子，functor)}

可以用作函数的对象，operator()在类中定义。所有Lambda都是函数对象。